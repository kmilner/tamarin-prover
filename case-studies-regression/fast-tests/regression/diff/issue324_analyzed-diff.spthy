theory Issue324

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Init:
   [ Fr( ~k ) ] --> [ !Tag( ~k ) ]

rule (modulo E) T_out:
   [ !Tag( k ) ] --[ PlayT( k ) ]-> [ Out( k ) ]

restriction singleSessionTag [right]:
  "¬(∃ k #i1 #i2.
      ((PlayT( k ) @ #i1) ∧ (PlayT( k ) @ #i2)) ∧ (¬(#i1 = #i2)))"
  // safety formula

  /*
  expanded formula:
  "¬(∃ k #i1 #i2.
      ((PlayT( k ) @ #i1) ∧ (PlayT( k ) @ #i2)) ∧ (¬(#i1 = #i2)))"
  */

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Destrd_0_fst
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  qed
next
  case Rule_Destrd_0_snd
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  qed
next
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case T_out
      step( solve( !Tag( t ) ▶₀ #vr ) )
        case Init
        by sorry // bound 5 hit
      qed
    qed
  qed
next
  case Rule_Init
  backward-search
    case LHS
    step( simplify )
    by sorry // Cannot prove
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_Send
  backward-search
    case LHS
    step( simplify )
    step( solve( !KU( x ) @ #vk ) )
      case c_fst
      step( solve( !KU( fst(x) ) @ #vk ) )
        case c_fst
        by sorry // bound 5 hit
      qed
    next
      case c_pair
      step( solve( !KU( x ) @ #vk.1 ) )
        case c_fst
        by sorry // bound 5 hit
      next
        case c_pair
        by sorry // bound 5 hit
      next
        case c_snd
        by sorry // bound 5 hit
      next
        case coerce
        by sorry // bound 5 hit
      next
        case fresh
        by sorry // bound 5 hit
      next
        case pub
        by sorry // bound 5 hit
      qed
    next
      case c_snd
      step( solve( !KU( snd(x) ) @ #vk ) )
        case c_snd
        by sorry // bound 5 hit
      qed
    next
      case coerce
      step( solve( !KD( x ) ▶₀ #vk ) )
        case T_out
        by sorry // bound 5 hit
      qed
    next
      case fresh
      step( solve( !KU( ~x ) @ #vk ) )
        case fresh
        by sorry // bound 5 hit
      qed
    next
      case pub
      step( solve( !KU( $x ) @ #vk ) )
        case pub
        by sorry // bound 5 hit
      qed
    qed
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_T_out
  backward-search
    case LHS
    step( simplify )
    step( solve( !Tag( k ) ▶₀ #i ) )
      case Init
      by sorry // Cannot prove
    qed
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: c1f66a89dd4225513a7d34f28e0696bd5031b9a7 (with uncommited changes), branch: msr-core-soundness
Compiled at: 2026-09-16 21:06:54.322462762 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/diff/issue324.spthy

  output:          examples/regression/diff/issue324.spthy.tmp
  processing time: 0.06s

  DiffLemma:  Observational_equivalence : analysis incomplete (61 steps)
  Equivalence proof may remain incomplete: preservation of these restrictions
  across multiple events could not be established:
    RHS: singleSessionTag
  These restrictions remain enforced. Side lemmas and attack search are still available.

==============================================================================
*/
