theory SoundnessDiffConditionalRestriction

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Emit:
   [ In( x ) ] --[ A( x ) ]-> [ Out( x ) ]

restriction BanA [right]:
  "∀ #i. (A( 'a' ) @ #i) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ #i. (A( 'a' ) @ #i) ⇒ (⊥)"
  */

restriction BanA [left]:
  "∀ #i. (A( 'a' ) @ #i) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ #i. (A( 'a' ) @ #i) ⇒ (⊥)"
  */

lemma left_witness [left]:
  exists-trace "∃ #i. A( 'b' ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (A( 'b' ) @ #i)"
*/
simplify
SOLVED // trace found

lemma right_witness [right]:
  exists-trace "∃ #i. A( 'b' ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (A( 'b' ) @ #i)"
*/
simplify
SOLVED // trace found

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Destrd_0_fst
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* forbidden chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* forbidden chain */ )
    qed
  qed
next
  case Rule_Destrd_0_snd
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* forbidden chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* forbidden chain */ )
    qed
  qed
next
  case Rule_Emit
  backward-search
    case LHS
    step( simplify )
    step( solve( !KU( x ) @ #vk ) )
      case c_fst
      step( solve( !KU( fst(x) ) @ #vk ) )
        case c_fst
        MIRRORED
      qed
    next
      case c_pair
      MIRRORED
    next
      case c_snd
      step( solve( !KU( snd(x) ) @ #vk ) )
        case c_snd
        MIRRORED
      qed
    next
      case coerce
      step( solve( !KD( x ) ▶₀ #vk ) )
        case Emit
        by step( contradiction /* forbidden chain */ )
      qed
    next
      case fresh
      step( solve( !KU( ~x ) @ #vk ) )
        case fresh
        MIRRORED
      qed
    next
      case pub
      step( solve( !KU( $x ) @ #vk ) )
        case pub
        MIRRORED
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KU( x ) @ #vk ) )
      case c_fst
      step( solve( !KU( fst(x) ) @ #vk ) )
        case c_fst
        MIRRORED
      qed
    next
      case c_pair
      MIRRORED
    next
      case c_snd
      step( solve( !KU( snd(x) ) @ #vk ) )
        case c_snd
        MIRRORED
      qed
    next
      case coerce
      step( solve( !KD( x ) ▶₀ #vk ) )
        case Emit
        by step( contradiction /* forbidden chain */ )
      qed
    next
      case fresh
      step( solve( !KU( ~x ) @ #vk ) )
        case fresh
        MIRRORED
      qed
    next
      case pub
      step( solve( !KU( $x ) @ #vk ) )
        case pub
        MIRRORED
      qed
    qed
  qed
next
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Emit
      step( solve( (#vl, 0) ~~> (#i, 1) ) )
        case Var_msg_t
        step( solve( !KU( t ) @ #vk ) )
          case c_fst
          step( solve( !KU( fst(x) ) @ #vk ) )
            case c_fst
            MIRRORED
          qed
        next
          case c_pair
          MIRRORED
        next
          case c_snd
          step( solve( !KU( snd(x) ) @ #vk ) )
            case c_snd
            MIRRORED
          qed
        next
          case coerce
          step( solve( !KD( t ) ▶₀ #vk ) )
            case Emit
            by step( contradiction /* forbidden chain */ )
          qed
        next
          case fresh
          step( solve( !KU( ~x ) @ #vk ) )
            case fresh
            MIRRORED
          qed
        next
          case pub
          step( solve( !KU( $x ) @ #vk ) )
            case pub
            MIRRORED
          qed
        qed
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Emit
      step( solve( (#vl, 0) ~~> (#i, 1) ) )
        case Var_msg_t
        step( solve( !KU( t ) @ #vk ) )
          case c_fst
          step( solve( !KU( fst(x) ) @ #vk ) )
            case c_fst
            MIRRORED
          qed
        next
          case c_pair
          MIRRORED
        next
          case c_snd
          step( solve( !KU( snd(x) ) @ #vk ) )
            case c_snd
            MIRRORED
          qed
        next
          case coerce
          step( solve( !KD( t ) ▶₀ #vk ) )
            case Emit
            by step( contradiction /* forbidden chain */ )
          qed
        next
          case fresh
          step( solve( !KU( ~x ) @ #vk ) )
            case fresh
            MIRRORED
          qed
        next
          case pub
          step( solve( !KU( $x ) @ #vk ) )
            case pub
            MIRRORED
          qed
        qed
      qed
    qed
  qed
next
  case Rule_Send
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: 38b1dae41cbfb0dcdb8e418102393dd2e070b589 (with uncommited changes), branch: msr-core-soundness
Compiled at: 2026-09-16 13:35:56.434328976 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/soundness-diff-conditional-restriction.spthy

  output:          examples/regression/trace/soundness-diff-conditional-restriction.spthy.tmp
  processing time: 0.08s

  LHS :  left_witness (exists-trace): verified (2 steps)
  RHS :  right_witness (exists-trace): verified (2 steps)
  DiffLemma:  Observational_equivalence : verified (78 steps)

==============================================================================
*/
