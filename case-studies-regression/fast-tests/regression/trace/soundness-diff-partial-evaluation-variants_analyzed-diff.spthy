theory SoundnessDiffPartialEvaluationVariants

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, sdec/2, senc/2, snd/1
equations:
    fst(<x.1, x.2>) = x.1,
    sdec(senc(x.1, x.2), x.2) = x.1,
    snd(<x.1, x.2>) = x.2

text{* the abstract state after partial evaluation contains 3 left facts:

1. Fr( ~z )

2. Out( z )

3. In( z )

Partial evaluation proposed 2 left refined multiset rewriting rules.
Note that the original number of multiset rewriting rules was 1.



 the abstract state after partial evaluation contains 3 right facts:

1. Fr( ~z )

2. Out( z )

3. In( z )

Partial evaluation proposed 1 right refined multiset rewriting rules.
Note that the original number of multiset rewriting rules was 1.

Diff partial evaluation is analysis-only; original equation-variant families are retained.
*}

rule (modulo E) Dec:
   [ In( x ), In( k ) ] --> [ Out( diff(sdec(x, k), x) ) ]

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Dec
      step( solve( splitEqs(0) ) )
        case split_case_1
        step( solve( !KU( senc(t, k) ) @ #vk.1 ) )
          case c_senc
          step( solve( (#vl, 0) ~~> (#i, 1) ) )
            case Var_msg_t
            step( solve( !KU( k ) @ #vk.2 ) )
              case c_fst
              step( solve( !KU( fst(x) ) @ #vk.1 ) )
                case c_fst
                step( solve( !KU( t ) @ #vk.2 ) )
                  case c_fst
                  step( solve( !KU( fst(x) ) @ #vk.2 ) )
                    case c_fst
                    step( solve( !KU( x.1 ) @ #vk.3 ) )
                      case fresh
                      step( solve( !KU( ~x.1 ) @ #vk.3 ) )
                        case fresh
                        step( solve( !KU( x ) @ #vk.4 ) )
                          case fresh
                          step( solve( !KU( ~x ) @ #vk.4 ) )
                            case fresh
                            by ATTACK // trace found
                          qed
                        qed
                      qed
                    qed
                  qed
                qed
              qed
            qed
          qed
        qed
      qed
    qed
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: 5eaca72490fca8990d2f04fbc4e6473375ffeb14 (with uncommited changes), branch: fix/diff-soundness
Compiled at: 2026-09-18 16:23:27.89974306 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/soundness-diff-partial-evaluation-variants.spthy

  output:          examples/regression/trace/soundness-diff-partial-evaluation-variants.spthy.tmp
  processing time: 0.31s

  DiffLemma:  Observational_equivalence : falsified - found trace (16 steps)

==============================================================================
*/
