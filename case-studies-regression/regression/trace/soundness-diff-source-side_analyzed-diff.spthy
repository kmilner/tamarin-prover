theory SoundnessDiffSourceSide

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Init:
   [ ] --[ A( diff('a', 'b') ) ]-> [ State( diff('a', 'b') ) ]

rule (modulo E) Finish:
   [ State( x ) ] --[ B( x ) ]-> [ ]

lemma left_source [left, sources]:
  all-traces "∀ x #i. (A( x ) @ #i) ⇒ (x = 'a')"
/*
guarded formula characterizing all counter-examples:
"∃ x #i. (A( x ) @ #i) ∧ ¬(x = 'a')"
*/
induction
  case empty_trace
  by contradiction /* from formulas */
next
  case non_empty_trace
  by contradiction /* from formulas */
qed

lemma right_counterexample [right]:
  all-traces "∀ #i. (B( 'b' ) @ #i) ⇒ (⊥)"
/*
guarded formula characterizing all counter-examples:
"∃ #i. (B( 'b' ) @ #i)"
*/
simplify
solve( State( 'b' ) ▶₀ #i )
  case Init
  SOLVED // trace found
qed

lemma right_witness [right]:
  exists-trace "∃ #i. B( 'b' ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (B( 'b' ) @ #i)"
*/
simplify
solve( State( 'b' ) ▶₀ #i )
  case Init
  SOLVED // trace found
qed

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Destrd_0_fst
  backward-search
    case LHS
    step( simplify )
    by step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
  next
    case RHS
    step( simplify )
    by step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
  qed
next
  case Rule_Destrd_0_snd
  backward-search
    case LHS
    step( simplify )
    by step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
  next
    case RHS
    step( simplify )
    by step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
  qed
next
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    by step( solve( !KD( x ) ▶₁ #i ) )
  next
    case RHS
    step( simplify )
    by step( solve( !KD( x ) ▶₁ #i ) )
  qed
next
  case Rule_Finish
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_Init
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_Send
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: c8f67ec1caa3e57e489eb70f7e01acb68f1b767e (with uncommited changes), branch: fix/diff-soundness
Compiled at: 2026-09-17 09:26:49.324027181 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/soundness-diff-source-side.spthy

  output:          examples/regression/trace/soundness-diff-source-side.spthy.tmp
  processing time: 0.05s
  
  LHS :  left_source (all-traces): verified (3 steps)
  RHS :  right_counterexample (all-traces): falsified - found trace (3 steps)
  RHS :  right_witness (exists-trace): verified (3 steps)
  DiffLemma:  Observational_equivalence : verified (31 steps)

==============================================================================
*/
