theory ExplicitSideMacros

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

macros: m( x ) =  x

rule (modulo E) Emit:
   [ ] --[ Emitted( 'a' ) ]-> [ Out( 'a' ) ]

lemma emitted [right]:
  exists-trace "∃ #i. Emitted( 'a' ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (Emitted( 'a' ) @ #i)"
*/
simplify
SOLVED // trace found

lemma emitted [left]:
  exists-trace "∃ #i. Emitted( 'a' ) @ #i"
/*
guarded formula characterizing all satisfying traces:
"∃ #i. (Emitted( 'a' ) @ #i)"
*/
simplify
SOLVED // trace found

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Destrd_0_fst
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* impossible chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* impossible chain */ )
    qed
  qed
next
  case Rule_Destrd_0_snd
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* impossible chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Emit
      by step( contradiction /* impossible chain */ )
    qed
  qed
next
  case Rule_Emit
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Emit
      step( solve( (#vl, 0) ~~> (#i, 1) ) )
        case Const_pub_a
        step( solve( !KU( 'a' ) @ #vk ) )
          case coerce
          step( solve( !KD( 'a' ) ▶₀ #vk ) )
            case Emit
            by step( contradiction /* impossible chain */ )
          qed
        next
          case pub
          MIRRORED
        qed
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Emit
      step( solve( (#vl, 0) ~~> (#i, 1) ) )
        case Const_pub_a
        step( solve( !KU( 'a' ) @ #vk ) )
          case coerce
          step( solve( !KD( 'a' ) ▶₀ #vk ) )
            case Emit
            by step( contradiction /* impossible chain */ )
          qed
        next
          case pub
          MIRRORED
        qed
      qed
    qed
  qed
next
  case Rule_Send
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: 5eaca72490fca8990d2f04fbc4e6473375ffeb14 (with uncommited changes), branch: fix/diff-soundness
Compiled at: 2026-09-18 16:23:27.89974306 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/diff-explicit-side-macros.spthy

  output:          examples/regression/trace/diff-explicit-side-macros.spthy.tmp
  processing time: 0.05s

  RHS :  emitted (exists-trace): verified (2 steps)
  LHS :  emitted (exists-trace): verified (2 steps)
  DiffLemma:  Observational_equivalence : verified (40 steps)

==============================================================================
*/
