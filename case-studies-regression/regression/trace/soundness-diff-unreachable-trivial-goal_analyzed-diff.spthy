theory SoundnessDiffUnreachableTrivialGoal

begin

// Function signature and definition of the equational theory E

functions: fst/1, pair/2, snd/1
equations: fst(<x.1, x.2>) = x.1, snd(<x.1, x.2>) = x.2

rule (modulo E) Produce:
   [ ] --[ P( ) ]-> [ F( 'a' ) ]

rule (modulo E) Unreachable:
   [ F( x ) ] --[ A( x ) ]-> [ Out( 'ok' ) ]

restriction BanA [right]:
  "∀ x #i. (A( x ) @ #i) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ x #i. (A( x ) @ #i) ⇒ (⊥)"
  */

restriction BanProduce [right]:
  "∀ #i. (P( ) @ #i) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ #i. (P( ) @ #i) ⇒ (⊥)"
  */

restriction BanProduce [left]:
  "∀ #i. (P( ) @ #i) ⇒ (⊥)"
  // safety formula

  /*
  expanded formula:
  "∀ #i. (P( ) @ #i) ⇒ (⊥)"
  */

lemma left_unreachable [left]:
  all-traces "∀ x #i. (A( x ) @ #i) ⇒ (⊥)"
/*
guarded formula characterizing all counter-examples:
"∃ x #i. (A( x ) @ #i)"
*/
simplify
by solve( F( x ) ▶₀ #i )

lemma right_unreachable [right]:
  all-traces "∀ x #i. (A( x ) @ #i) ⇒ (⊥)"
/*
guarded formula characterizing all counter-examples:
"∃ x #i. (A( x ) @ #i)"
*/
simplify
by contradiction /* from formulas */

diffLemma Observational_equivalence:
rule-equivalence
  case Rule_Destrd_0_fst
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Unreachable
      by step( contradiction /* impossible chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Unreachable
      by step( contradiction /* impossible chain */ )
    qed
  qed
next
  case Rule_Destrd_0_snd
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Unreachable
      by step( contradiction /* impossible chain */ )
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( <x, x.1> ) ▶₀ #i ) )
      case Unreachable
      by step( contradiction /* impossible chain */ )
    qed
  qed
next
  case Rule_Equality
  backward-search
    case LHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Unreachable
      step( solve( (#vl, 0) ~~> (#i, 1) ) )
        case Const_pub_ok
        by step( solve( F( x ) ▶₀ #vr ) )
      qed
    qed
  next
    case RHS
    step( simplify )
    step( solve( !KD( x ) ▶₁ #i ) )
      case Unreachable
      by step( contradiction /* from formulas */ )
    qed
  qed
next
  case Rule_Produce
  backward-search
    case LHS
    step( simplify )
    by step( contradiction /* from formulas */ )
  next
    case RHS
    step( simplify )
    by step( contradiction /* from formulas */ )
  qed
next
  case Rule_Send
  backward-search
    case LHS
    step( simplify )
    MIRRORED
  next
    case RHS
    step( simplify )
    MIRRORED
  qed
next
  case Rule_Unreachable
  backward-search
    case LHS
    step( simplify )
    by step( solve( F( x ) ▶₀ #i ) )
  next
    case RHS
    step( simplify )
    by step( contradiction /* from formulas */ )
  qed
qed

/* All wellformedness checks were successful. */

/*
Generated from:
Tamarin version 1.13.0
Maude version 3.5.1
Git revision: c8f67ec1caa3e57e489eb70f7e01acb68f1b767e (with uncommited changes), branch: fix/diff-soundness
Compiled at: 2026-09-17 09:26:49.324027181 UTC
*/

end
/* Output

==============================================================================
summary of summaries:

analyzed: examples/regression/trace/soundness-diff-unreachable-trivial-goal.spthy

  output:          examples/regression/trace/soundness-diff-unreachable-trivial-goal.spthy.tmp
  processing time: 0.05s
  
  LHS :  left_unreachable (all-traces): verified (2 steps)
  RHS :  right_unreachable (all-traces): verified (2 steps)
  DiffLemma:  Observational_equivalence : verified (38 steps)

==============================================================================
*/
