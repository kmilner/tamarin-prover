theory SapicScopeLookupBranches
begin

// The lookup binder is local to the success branch; the failure branch binds its own x.
process: in(y); lookup 'key' as x in out(<x,y>) else in(x); out(<x,y>)

end
